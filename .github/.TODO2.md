- Add auth
- Link path correctly for app/src/main/assets/express.js to import sockets from '../bin/network_sockets.so'; using the right build path (need to instruct LLM this)
- Add most recent qjs version (could make a repo + workflow for it with maybe a cron to detect changes to know when to trigger a workflow by sending an update tag)
